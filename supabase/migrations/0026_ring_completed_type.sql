-- ============================================================================
-- 0026_ring_completed_type.sql — 「輪が閉じた」通知の型を足す（2026-08-12 項目9）
--
-- 収穫でできた玉突きの輪は、全員が受け取り終わってはじめて完成する。
-- そのとき参加者全員にお祝いを届けたいので、専用の型を用意する。
--
-- enum への値追加は同じトランザクション内では使えないため、
-- 追加だけをこのファイルに分けている（0024 と同じ理由）。
-- ============================================================================

alter type notification_type add value if not exists 'ring_completed';
