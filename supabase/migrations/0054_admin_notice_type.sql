-- ============================================================================
-- 0054_admin_notice_type.sql — 通知の種類を1つ足す（型だけ）
--
-- 2026-09-17 の指摘：通報を「対応済み」にしたとき、相手が何をされたのか・
-- その後どうなるのかが分からない。一般的なアプリと同じ作りにしてほしい。
--
-- 運営から本人へ「あなたの投稿を非表示にしました」「規約違反の警告です」
-- 「利用を停止しました」を届けるための種別。
--
-- enum に足した値は同じトランザクションでは使えないので、使う側（0055）と分ける。
-- ============================================================================

alter type notification_type add value if not exists 'admin_notice';
