-- ============================================================================
-- 0052_board_like_type.sql — 通知の種類を1つ足す（型だけ）
--
-- 2026-09-16 の実機テストの指摘：
--   「掲示板の投稿にいいねができるが、いいねされた側に通知が来ない。
--     コメントと同じように相手に通知が届くようにしてほしい」
--
-- enum に足した値は、同じトランザクションの中では使えない。
-- 0043→0044、0046→0047 と同じように、値の追加と、それを使う関数を別ファイルに分ける。
-- ============================================================================

alter type notification_type add value if not exists 'board_like';
