-- ============================================================================
-- 0046_rate_request_type.sql — 通知の種類を2つ足す（型だけ）
--
-- 2026-09-09 の会議の指摘：
--   ・取引の評価をしてください、という通知が通知一覧に来ない
--   ・受け取っただけで対応完了のように見えており、評価がフローに入っていない
--   ・自分に関連する情報は通知が行くように（いいねだけ通知が無かった）
--
-- enum に足した値は、同じトランザクションの中では使えない。
-- 0043→0044 と同じように、値の追加と、それを使う関数を別ファイルに分ける。
-- ============================================================================

alter type notification_type add value if not exists 'rate_request';
alter type notification_type add value if not exists 'item_like';
