-- ============================================================================
-- 0034_contact_email.sql — お問い合わせ先を管理画面から変えられるようにする
--
-- アプリの「お問い合わせ」が support@gungun.app という、
-- 実在しないドメインのアドレスのままだった（2026-08-13 指摘）。
-- 連絡先はリリース後も変わりうるので、アプリに焼き込まず設定に持たせる。
-- ============================================================================

insert into app_settings (key, value) values
  ('contact_email', '"warashibe.gungun@gmail.com"'::jsonb)
on conflict (key) do nothing;
