# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# 生の会話ログ。176MB 級で、APIキーやパスワードがそのまま入っているためコミットしない。
# 読める形は docs/logs/ に、伏せ字をかけて書き出す（npm run log:export）
*.jsonl

# env
.env
.env.local
.env*.local
.env.mock-stash
# 接続先を切り替えるときに作る控え。鍵が入るので絶対に上げない
.env*.bak-*

# IDE
.vscode/
.idea/
shots/
scratch-gallery.html
gungun-preview.html
shots-audit/
shots-water/
shots-tree/

# 実DB接続ビルドの出力（dist と同じく成果物なのでコミットしない）
dist-live/
